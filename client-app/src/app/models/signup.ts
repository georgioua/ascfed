export interface ISingUpFormValues {
    email: string;
}

export interface ICustomer {
    email: string;
    customerId: string;
}

export interface ISingUp {
    email: string;
    customerId: string;
}
